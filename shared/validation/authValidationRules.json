{
  "fullName": {
    "pattern": "^[A-Za-z]+(?:[ '-][A-Za-z]+)*$",
    "minLength": 2,
    "maxLength": 30,
    "message": "Full name must contain only letters (2-30 characters)."
  },
  "password": {
    "minLength": 8,
    "minLengthMessage": "Password must be at least 8 characters.",
    "complexityMessage": "Password must include uppercase, lowercase, and a number."
  }
}
